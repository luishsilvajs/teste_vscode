// Functional declaration -- in this way load all functions before execute program
function soma(a, b){
    return a + b
}

// Function Expression -- in this way, it depends on the declaration of the function before running the program
const sub = function(x, y){
    return x - y
}

// Named function expression
const mult = function mult(x, y) {
    return x * y
}